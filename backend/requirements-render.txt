# Core
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-dotenv==1.2.1
pydantic==2.12.5

# Database
motor==3.3.1           # Driver async para Mongo
pymongo==4.10.1        # Versión compatible con motor 3.3.1
aiomysql==0.2.0
PyMySQL==1.1.2

# HTTP Client
httpx==0.28.1
aiohttp==3.9.5

# LLM / RouteLLM (si estás usando la opción SDK)
openai==1.61.1

# Auth & Security
cryptography==44.0.1

# Google AI (solo si realmente lo usas; si no, coméntalas)
google-generativeai==0.8.6
google-genai==1.62.0

# IMPORTANTE: litellm fuera o sin versión fija
# litellm   # <- Solo si de verdad lo usas en tu código. Si no, elimínalo.
